# PROJECT KNOWLEDGE SYNC MANIFEST

## Base: v1 --- 2026-03-07

Total files: 0
Current version: v1

## Version Log

| Version | Date | Trigger | Files Changed |
|---------|------|---------|---------------|
| v1 | 2026-03-07 | Initial build | All files |

## Instructions

1. **Initial Upload:** Upload the entire contents of `sync/v1/` to your cloud agent project.
2. **Mark as Synced:** After upload, run `ciphergy.sh mark-synced` to record the sync state.
3. **Subsequent Updates:** Run `ciphergy.sh project-push` to generate a delta version folder.
4. **Upload Delta:** Upload only the new version folder (e.g., `sync/v2/`) to the cloud agent.
5. **Mark Again:** Run `ciphergy.sh mark-synced` after each upload.

## Sync Rules

- Each version folder contains ONLY the files that changed since the last sync.
- The registry tracks MD5 hashes to detect changes automatically.
- Never modify files inside `sync/v*/` folders directly.
- Always upload version folders in order (v1, v2, v3, ...).
- The cloud agent should replace existing files with newer versions, not append.

## Verification

After uploading, verify the cloud agent has the correct files by:
1. Running `ciphergy.sh sync-check` locally
2. Asking the cloud agent to confirm receipt of the uploaded files
3. Comparing file counts and names between local and cloud

---
*Generated by Ciphergy Pipeline*
